//===========================================================================
// Validations
// Removing of redundant and mutually exclusive flags to prevent generating
// of the vertex/pixel shaders
// TODO: make special validations for PS.1.1 only support

#if !%ENVIRONMENT_MAP
  #warning Parameter %ENVIRONMENT_MAP was ignored
#endif

#if !%GLOSS_MAP
  #warning Parameter %GLOSS_MAP was ignored
#endif

#if !%BUMP_MAP
  #warning Parameter %BUMP_MAP was ignored
#endif
